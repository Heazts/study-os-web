# fastapi==0.115.6 travava starlette em <0.42.0 via dependencia transitiva -
# uma faixa vulneravel ao CVE-2026-48710 ("BadHost": Host header nao validado
# usado para reconstruir request.url, permitindo bypass de checagens de
# seguranca baseadas nessa URL em algumas configuracoes). Atualizado + pin
# explicito do starlette corrigido, testado contra a suite inteira.
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.12.0
PyJWT==2.10.1
bcrypt==4.3.0
# python-multipart==0.0.9 tinha DoS (CVE-2024-53981, complexidade de regex no
# parsing de multipart/form-data) corrigido na 0.0.18+.
python-multipart==0.0.32
httpx==0.28.1
# Atualizado por precaucao (correcoes de seguranca no parsing de imagem) -
# este projeto processa uploads de imagem nao confiaveis (avatar/scan).
Pillow==12.3.0
# Driver para Postgres (ex.: DATABASE_URL apontando para Supabase/Neon) -
# so' e' realmente importado quando DATABASE_URL usa postgresql://; com
# sqlite:// (padrao) esta dependencia fica sem uso, mas nao tem custo manter
# instalada (ja vem como wheel binario, sem precisar de libpq no sistema).
psycopg2-binary==2.9.10
